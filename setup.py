from setuptools import setup

setup(name='reuseable_runner',
      version='0.1',
      description='Test Runner to be used with django_jenkins that allows for database reuse',
      url='http://github.com/brendancustommade/reuseable_runner',
      author='brendan',
      author_email='brendan@custommade.com',
      license='MIT',
      packages=['reuseable_runner'],
      zip_safe=False)